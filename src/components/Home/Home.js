import React from 'react';
import {storage} from '../../firebase/index';

const Home = () => {
    return (
        <div>
            <header></header>
            <main></main>
        </div>
    );
};

export default Home;